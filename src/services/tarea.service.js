const API_BASE_URL = 'http://127.0.0.1:8000/api';

const tareaService = {

  getAll: async () => {
  },

  create: async (tarea) => {
  },

  update: async (id, tarea) => {
  },

  delete: async (id) => {
  },

  getOne: async (id) => {
  },

  toggle: async (id) => {
  }
};

export default tareaService;